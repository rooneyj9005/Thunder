# .packwizignore uses the same syntax as .gitignore and is evaluated
# relative to the pack root. Any path matched here will be omitted from
# the exported .mrpack so clients never download it.
# See: https://git-scm.com/docs/gitignore
/docs/
/CNAME
/README.md
/LICENSE
/.git/
/.gitignore
/.gitattributes
/.github/
/.packwizignore
/pterodactyl.json
/install.sh
/install.ps1

# Keep these scripts in the exported pack (do not ignore):
# /startup.sh
# /startup.ps1
# /update.sh
# /update.ps1
# /start-local.ps1